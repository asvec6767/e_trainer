# E-trainer Frontend

Фронтенд приложения E-trainer на Vue 3 с PrimeVue.

## Технологии

- Vue 3 (Composition API)
- PrimeVue 4.0
- Vue Router
- Vite

## Установка зависимостей

```bash
npm install
```

## Разработка

Для запуска в режиме разработки:

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:5173`

## Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `../static`, откуда их будет раздавать Go сервер.

## Структура проекта

```
frontend/
├── src/
│   ├── assets/              # Статические ресурсы (CSS, изображения)
│   ├── components/          # Переиспользуемые компоненты
│   │   ├── layout/          # Компоненты макета (Header, Footer)
│   │   ├── common/          # Общие компоненты
│   │   └── ui/              # UI компоненты
│   ├── views/               # Страницы приложения (маршруты)
│   │   └── HomeView.vue     # Пример страницы
│   ├── router/              # Конфигурация роутера
│   ├── App.vue              # Корневой компонент
│   └── main.js              # Точка входа
├── index.html               # HTML шаблон
├── vite.config.js           # Конфигурация Vite
└── package.json             # Зависимости проекта
```

## Организация файлов

### 📄 Страницы (`src/views/`)
Здесь создавайте страницы (views), которые соответствуют маршрутам приложения.

**Пример:** `HomeView.vue`, `AboutView.vue`, `ContactView.vue`

### 🧩 Компоненты

#### Layout компоненты (`src/components/layout/`)
Компоненты макета сайта: Header, Footer, Sidebar и т.д.

**Текущие компоненты:**
- `Header.vue` - Шапка сайта с навигацией
- `Footer.vue` - Подвал сайта

#### Общие компоненты (`src/components/common/`)
Переиспользуемые компоненты, используемые в разных частях приложения.

**Примеры:** `LoadingSpinner.vue`, `ErrorMessage.vue`, `EmptyState.vue`

#### UI компоненты (`src/components/ui/`)
Кастомные UI компоненты, которые дополняют PrimeVue.

**Примеры:** `CustomButton.vue`, `CustomInput.vue`, `Modal.vue`

## PrimeVue

Проект использует PrimeVue с темой Aura. Подробнее: https://primevue.org/

